[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
vm-memory = { git = "https://github.com/tylerfanelli/vm-memory.git", branch = "as_slice", features = ["backend-mmap"] }

utils = { path = "../utils" }
